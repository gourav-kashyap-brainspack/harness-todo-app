# Canonical Patterns Registry — &lt;PROJECT_NAME&gt;

> **Tier-1 cross-cutting patterns** — the recurring *how* that must be identical across tasks (response envelope, DTO/validation style, auth guard, ownership checks, pagination, data-access, module layout, forms, async UI states…). Established by the first task that needs each, reused thereafter.
>
> **This file is split out of `conventions.md` to keep always-loaded context lean.** It is NOT `@`-imported — it is read on demand. **Builders, architect, code-reviewer, and librarian MUST `Read` this file before classifying / inventing / reviewing any Tier-1 pattern.** The *policy* that governs this registry (two-tier rule + roles) lives in `conventions.md` → "Pattern policy (two-tier)".
>
> **Maintained by the librarian** (append a row on task done). `ref:` = the reference implementation to copy. **Empty until the first foundation task lands** — the first task to solve a Tier-1 concern well establishes the pattern; every later task reuses it rather than re-deciding.

| pattern | rule (one line) | ref (file:symbol) | established by | ADR |
|---|---|---|---|---|
| Feature-sliced folder structure + `@/*` alias | `mobile/src/{app,core,features,components/ui,platform,theme}`; import via `@/…`, never deep-relative `../../..` | `mobile/tsconfig.json` (`paths`) + `mobile/babel.config.js` (`module-resolver`) | FND-001 | — |
| eslint-plugin-boundaries layer config | Element types `app/feature/core/components/theme/platform` by path; allowed direction `app→feature→core|components|theme|platform`, no cross-feature imports, default `disallow`, violation = `error` (gate is `--max-warnings=0`) | `mobile/.eslintrc.js` (`settings['boundaries/elements']`, `rules['boundaries/dependencies']`) | FND-001 | — |
| NativeWind `className` styling | Style via Tailwind `className`; `StyleSheet.create` only as an escape hatch for third-party native wrappers NativeWind can't intercept (e.g. `GestureHandlerRootView`) | `mobile/tailwind.config.js` (content globs + `nativewind/preset`) + `mobile/babel.config.js` (`nativewind/babel`) + `mobile/metro.config.js` (`withNativeWind`) + `mobile/src/app/AppProviders.tsx` (StyleSheet escape-hatch example) | FND-001 | — |
| AppProviders composition root | One root component in `src/app/` wraps the provider stack (`GestureHandlerRootView` → `SafeAreaProvider` → …); later foundation tasks (theme, nav) add a layer each rather than each screen mounting its own providers; no network/query provider mounted while local-only (OQ-1) | `mobile/src/app/AppProviders.tsx:AppProviders` | FND-001 | — |
| Semantic design-token theming | Colors are CSS vars (`rgb(var(--color-x) / <alpha-value>)`) declared once in `tailwind.config.js` `theme.extend.colors`, values set per palette in `global.css` (`:root` = light, `.dark` = dark); `darkMode: 'class'` (required — `media` throws on a manual `colorScheme.set`). Components use `className="bg-bg text-text …"` token names only — **never** raw hex or a `dark:`-prefixed color variant | `mobile/tailwind.config.js` (`theme.extend.colors/fontSize/borderRadius`) + `mobile/global.css` (`:root`/`.dark`) | FND-002 | — |
| Theme store + provider (UI-state store w/ MMKV preference) | Zustand store holds `mode: 'system'\|'light'\|'dark'` + derived `resolvedScheme`; a raw single-key MMKV read (guarded by a type-guard, safe `'system'` default) restores `mode` at module-load time (before first render, no async gate); `ThemeProvider` forwards live `useColorScheme()` changes into the store (no-op unless `mode==='system'`) and pushes `resolvedScheme` into NativeWind's `colorScheme.set(...)`; `useTheme()` (with `useShallow`) is the only hook screens consume. Mounted once in `AppProviders`. This is the reference for any future UI-state store that needs a persisted preference (e.g. STG) | `mobile/src/core/store/themeStore.ts:useThemeStore,readPersistedMode` + `mobile/src/theme/ThemeProvider.tsx:ThemeProvider` + `mobile/src/theme/useTheme.ts:useTheme` | FND-002 | — |
| MMKV raw-preference persistence | For a single scalar preference with no typed storage service yet: one raw MMKV string key, a type-guard on read (`isThemeMode`), fallback to a safe default on absent/corrupt data, synchronous write on every mutation (no debounce needed at this scale). Adopt as-is until a typed STG storage service exists, then migrate | `mobile/src/core/store/themeStore.ts:readPersistedMode,THEME_MODE_STORAGE_KEY` | FND-002 | — |
| Typed navigation pattern | Route params typed once via `RootStackParamList`/`TabParamList` + a `declare global { namespace ReactNavigation { interface RootParamList extends RootStackParamList {} } }` augmentation (v6 "dynamic"/JSX-navigator typing — not the v7+ static-config API), so every bare `useNavigation()`/`useRoute()` call app-wide infers `RootStackParamList` with no per-call generic. Screens that need explicit route-narrowing use the typed `useAppNavigation<RouteName>()`/`useAppRoute<RouteName>()` helpers instead of importing `useNavigation`/`useRoute` directly. PRO/TSK screens reuse this — never re-declare param types or re-augment the global | `mobile/src/app/navigation/types.ts` + `mobile/src/app/navigation/hooks.ts:useAppNavigation,useAppRoute` | FND-003 | — |
| Native-prop color resolution (`nativeChromeColors` — general rule, not just nav chrome) | **Any** native `color`/tint prop NativeWind `className` can't reach (nav container/tab-bar tint, OS status bar, `ActivityIndicator.color`, a vector-icon's `color`) resolves its RGB from the **one** documented source, `src/theme/nativeChromeColors.ts` (`NATIVE_CHROME_RGB[resolvedScheme]` + `rgbFromTriplet(...)`) — values copied **verbatim** from `design-system.md`'s color tables, never a fresh hardcoded hex. `navigationTheme.ts`, `TabNavigator.tsx`, `ThemeProvider.tsx`'s StatusBar (FND-003), and `EmptyState.tsx`/`LoadingIndicator.tsx` (FND-005) all consume this single map. A palette change is one edit here, never per-consumer copies. **This is the one place to add a new native-chrome/native-prop color** — do not hand-roll another RGB map or literal hex for a native-only surface (candidate for a future grep-guard against fresh native-color hex literals if this recurs as a review finding) | `mobile/src/theme/nativeChromeColors.ts:NATIVE_CHROME_RGB,rgbFromTriplet` + `mobile/src/app/navigation/navigationTheme.ts:buildNavigationTheme` + `mobile/src/components/ui/{EmptyState.tsx,LoadingIndicator.tsx}` | FND-003 (extended FND-005) | — |
| Shared UI primitives (reuse-or-block) | `EmptyState` / `LoadingIndicator` / `Screen` in `src/components/ui` (barrel `@/components/ui`) are the single implementation of each presentational concern — features **reuse**, never re-implement; a fresh re-implementation of one of these is a `[blocking]` code-review finding. Confirmed consumers: F-031 (ORG, no-search-results) reuses `EmptyState`; F-042 (TSK, pull-to-refresh) reuses `LoadingIndicator` | `mobile/src/components/ui/{EmptyState.tsx,LoadingIndicator.tsx,Screen.tsx,index.ts}` | FND-005 | — |
| App icon family = Feather (`FeatherIconName`) | `react-native-vector-icons/Feather` is the app-wide icon family (first use in FND-005, adopt app-wide — don't add a second icon font). Icon `name` props are typed via the `FeatherIconName` type-only alias (`keyof typeof import('react-native-vector-icons/glyphmaps/Feather.json')`), recovering a real union from the installed glyph map — reuse this alias for any future `icon` prop rather than re-deriving or falling back to a bare `string` | `mobile/src/components/ui/EmptyState.tsx:FeatherIconName` | FND-005 | — |
| Themed StatusBar (OS chrome, not nav chrome) | The OS status bar (glyph color + Android background) is rendered from `ThemeProvider`, not from the navigator — `NavigationContainer`'s `theme.dark` only styles React Navigation's own chrome (headers/tab-bar/background), it does not drive the OS status bar. Flips `barStyle`/`backgroundColor` with `resolvedScheme`, reusing the same `NATIVE_CHROME_RGB` source above, so it holds even before/without a navigator mounted | `mobile/src/theme/ThemeProvider.tsx:ThemeProvider` | FND-003 | — |
| Placeholder screen + RN-Navigation jest setup | Every unbuilt route renders a themed `<PlaceholderScreen name detail?>` (semantic tokens only) until its feature module replaces it — navigator wiring never changes when that swap happens, it only imports the screen component. Jest env fixes for anything that mounts through React Navigation/SafeAreaProvider are wired **once, globally** in `mobile/jest/setup.js` (registered in `jest.config.js` `setupFiles`) — currently the official `react-native-safe-area-context/jest/mock` (real `SafeAreaProvider` never fires initial-insets under Jest, so nested trees render `null` without it). Any future screen test that mounts through `AppProviders`/navigation reuses this setup file rather than re-mocking safe-area per test | `mobile/src/app/navigation/PlaceholderScreen.tsx` + `mobile/jest/setup.js` (wired via `mobile/jest.config.js:setupFiles`) | FND-003 | — |
| App bootstrap / first-launch seam (MMKV flag, single writer) | `hasLaunched` is one raw MMKV boolean key (`app.hasLaunched`), read via a guarded `readHasLaunched()` (try/catch, defaults `false` — survives a *throwing* read, not just a bad type, per FR6) and seeded once as `useLaunchStore`'s initial state. **`BootstrapScreen` (the `Splash` route) only READS the flag** to `navigation.reset` into `ProfileSetup` (false) or `Tabs` (true) — it never calls `setHasLaunched()` itself. `setHasLaunched()` is the single seam **PRO** calls once first-run profile setup genuinely completes. **Load-bearing invariant: never add a second source of truth for "has this device launched before" — PRO writes through this store, it does not track its own flag.** | `mobile/src/core/store/launchStore.ts:useLaunchStore,readHasLaunched,setHasLaunched` + `mobile/src/app/navigation/BootstrapScreen.tsx:BootstrapScreen` | FND-004 | — |
| Native splash (react-native-bootsplash) config | Both platforms wired end-to-end: Android `res/values/{colors.xml,styles.xml}` (`BootTheme`) + `AndroidManifest.xml` activity theme + `MainActivity.kt` (`RNBootSplash.init()` before `super.onCreate(null)` — the documented "screens < 4.16.0" integration, since this app pins `react-native-screens@4.8.0`); iOS `BootSplash.storyboard` + `AppDelegate.mm` (`customizeRootView:` → `RNBootSplash initWithStoryboard:rootView:`, the pre-RN-0.80 Obj-C `RCTAppDelegate` subclass form) + `Info.plist` (`UILaunchStoryboardName` = `BootSplash`). JS side hides it with `BootSplash.hide({fade: true})` once the boot decision (theme + launch flag) resolves. **This is the reference config for both platforms** — the current asset is still the library's placeholder mark; swapping in a real logo + running `bootsplash generate` is a deferred design follow-up, not yet done. | `mobile/android/app/src/main/res/values/{colors.xml,styles.xml}` + `mobile/android/app/src/main/java/com/todoapp/MainActivity.kt` + `mobile/ios/TodoApp/{BootSplash.storyboard,AppDelegate.mm,Info.plist}` | FND-004 | — |
