# Canonical Patterns Registry — &lt;PROJECT_NAME&gt;

> **Tier-1 cross-cutting patterns** — the recurring *how* that must be identical across tasks (response envelope, DTO/validation style, auth guard, ownership checks, pagination, data-access, module layout, forms, async UI states…). Established by the first task that needs each, reused thereafter.
>
> **This file is split out of `conventions.md` to keep always-loaded context lean.** It is NOT `@`-imported — it is read on demand. **Builders, architect, code-reviewer, and librarian MUST `Read` this file before classifying / inventing / reviewing any Tier-1 pattern.** The *policy* that governs this registry (two-tier rule + roles) lives in `conventions.md` → "Pattern policy (two-tier)".
>
> **Maintained by the librarian** (append a row on task done). `ref:` = the reference implementation to copy. **Empty until the first foundation task lands** — the first task to solve a Tier-1 concern well establishes the pattern; every later task reuses it rather than re-deciding.

| pattern | rule (one line) | ref (file:symbol) | established by | ADR |
|---|---|---|---|---|
| Feature-sliced folder structure + `@/*` alias | `mobile/src/{app,core,features,components/ui,platform,theme}`; import via `@/…`, never deep-relative `../../..` | `mobile/tsconfig.json` (`paths`) + `mobile/babel.config.js` (`module-resolver`) | FND-001 | — |
| eslint-plugin-boundaries layer config | Element types `app/feature/core/components/theme/platform` by path; allowed direction `app→feature→core|components|theme|platform`, no cross-feature imports, default `disallow`, violation = `error` (gate is `--max-warnings=0`) | `mobile/.eslintrc.js` (`settings['boundaries/elements']`, `rules['boundaries/dependencies']`) | FND-001 | — |
| NativeWind `className` styling | Style via Tailwind `className`; `StyleSheet.create` only as an escape hatch for third-party native wrappers NativeWind can't intercept (e.g. `GestureHandlerRootView`) | `mobile/tailwind.config.js` (content globs + `nativewind/preset`) + `mobile/babel.config.js` (`nativewind/babel`) + `mobile/metro.config.js` (`withNativeWind`) + `mobile/src/app/AppProviders.tsx` (StyleSheet escape-hatch example) | FND-001 | — |
| AppProviders composition root | One root component in `src/app/` wraps the provider stack (`GestureHandlerRootView` → `SafeAreaProvider` → …); later foundation tasks (theme, nav) add a layer each rather than each screen mounting its own providers; no network/query provider mounted while local-only (OQ-1) | `mobile/src/app/AppProviders.tsx:AppProviders` | FND-001 | — |
| Semantic design-token theming | Colors are CSS vars (`rgb(var(--color-x) / <alpha-value>)`) declared once in `tailwind.config.js` `theme.extend.colors`, values set per palette in `global.css` (`:root` = light, `.dark` = dark); `darkMode: 'class'` (required — `media` throws on a manual `colorScheme.set`). Components use `className="bg-bg text-text …"` token names only — **never** raw hex or a `dark:`-prefixed color variant | `mobile/tailwind.config.js` (`theme.extend.colors/fontSize/borderRadius`) + `mobile/global.css` (`:root`/`.dark`) | FND-002 | — |
| Theme store + provider (UI-state store w/ MMKV preference) | Zustand store holds `mode: 'system'\|'light'\|'dark'` + derived `resolvedScheme`; a raw single-key MMKV read (guarded by a type-guard, safe `'system'` default) restores `mode` at module-load time (before first render, no async gate); `ThemeProvider` forwards live `useColorScheme()` changes into the store (no-op unless `mode==='system'`) and pushes `resolvedScheme` into NativeWind's `colorScheme.set(...)`; `useTheme()` (with `useShallow`) is the only hook screens consume. Mounted once in `AppProviders`. This is the reference for any future UI-state store that needs a persisted preference (e.g. STG) | `mobile/src/core/store/themeStore.ts:useThemeStore,readPersistedMode` + `mobile/src/theme/ThemeProvider.tsx:ThemeProvider` + `mobile/src/theme/useTheme.ts:useTheme` | FND-002 | — |
| MMKV raw-preference persistence — **superseded by "Typed storage service" below for anything new** | For a single scalar preference with no typed storage service yet: one raw MMKV string key, a type-guard on read (`isThemeMode`), fallback to a safe default on absent/corrupt data, synchronous write on every mutation (no debounce needed at this scale). **This was the FND-era pattern before STG-001 existed; `themeStore`/`launchStore` are the grandfathered exception (unchanged, same default MMKV instance/keys per spec-gap b) — do NOT use this raw-key shape for any new persisted value, use the typed storage service instead.** **Confirmed followed: ORG-001's `taskQueryStore` is the first new persisted UI preference since STG-002 and correctly used the typed service (see the "UI-query-state via typed storage service" row below), not this shape** | `mobile/src/core/store/themeStore.ts:readPersistedMode,THEME_MODE_STORAGE_KEY` | FND-002 | — |
| Typed storage service (the ONE persistence path) | All domain persistence goes through `core/services/storage.ts`'s `getItem<T>(key,schema,fallback)`/`setItem`/`removeItem`/`hasItem` — no feature/domain code imports `react-native-mmkv` directly (code-reviewer blocks it). Every value is wrapped in a `{version,data}` envelope with a `migrate(version,data)` seam for future schema changes; every read is **total** (absent key · malformed JSON · non-envelope shape · unmigratable version · failed Zod parse · a thrown native read all fall back to the caller's `fallback`, never throw — F-038). Keys are declared once in the `StorageKeys` registry (single source of truth, prevents key-string drift). Two hard-won correctness rules for anyone extending this: **(a)** the "can't migrate this version" sentinel MUST be a `Symbol` (`UNMIGRATABLE`), never `null` — `null` is a legitimate persisted `data` value (e.g. a nullable Zod schema), so reusing it as a corrupt-marker would wrongly discard real data; **(b)** the corrupt/thrown-read path logs `error.name` only, never `String(error)`/`error.message`/the raw stored string — a `JSON.parse` error message can echo a fragment of the corrupt blob, which may contain PII once STG-002+ payloads (profile name/email) land | `mobile/src/core/services/storage.ts:getItem,setItem,removeItem,hasItem,StorageKeys,migrate` + `mobile/src/core/types/storage.ts:StorageEnvelope` | STG-001 | — |
| Store hydration pattern (Zustand ⇄ typed storage) | `createPersistedValue(key, schema, fallback)` returns `{hydrate, persist}`: a Zustand store calls `.hydrate()` once at store-init to seed its persisted slice (synchronous, safe on the cold-start path) and calls `.persist(value)` inside its mutator actions to write back. This is what STG-002 (Profile/Task repositories) and any later PRO/TSK persisted state copy verbatim — do not hand-roll a new hydrate/persist pair per store | `mobile/src/core/services/storage.ts:createPersistedValue` + `mobile/src/core/types/storage.ts:PersistedValue` | STG-001 | — |
| Typed navigation pattern | Route params typed once via `RootStackParamList`/`TabParamList` + a `declare global { namespace ReactNavigation { interface RootParamList extends RootStackParamList {} } }` augmentation (v6 "dynamic"/JSX-navigator typing — not the v7+ static-config API), so every bare `useNavigation()`/`useRoute()` call app-wide infers `RootStackParamList` with no per-call generic. Screens that need explicit route-narrowing use the typed `useAppNavigation<RouteName>()`/`useAppRoute<RouteName>()` helpers instead of importing `useNavigation`/`useRoute` directly. PRO/TSK screens reuse this — never re-declare param types or re-augment the global | `mobile/src/app/navigation/types.ts` + `mobile/src/app/navigation/hooks.ts:useAppNavigation,useAppRoute` | FND-003 | — |
| Native-prop color resolution (`nativeChromeColors` — general rule, not just nav chrome) | **Any** native `color`/tint prop NativeWind `className` can't reach (nav container/tab-bar tint, OS status bar, `ActivityIndicator.color`, a vector-icon's `color`) resolves its RGB from the **one** documented source, `src/theme/nativeChromeColors.ts` (`NATIVE_CHROME_RGB[resolvedScheme]` + `rgbFromTriplet(...)`) — values copied **verbatim** from `design-system.md`'s color tables, never a fresh hardcoded hex. `navigationTheme.ts`, `TabNavigator.tsx`, `ThemeProvider.tsx`'s StatusBar (FND-003), and `EmptyState.tsx`/`LoadingIndicator.tsx` (FND-005) all consume this single map. A palette change is one edit here, never per-consumer copies. **This is the one place to add a new native-chrome/native-prop color** — do not hand-roll another RGB map or literal hex for a native-only surface (candidate for a future grep-guard against fresh native-color hex literals if this recurs as a review finding). **Extended (TSK-004):** the map's `success` entry (already declared as a token in `global.css`, just never mirrored here) was added alongside the existing `primary`/`danger` entries — needed because TSK-004's Detail toggle button and the row/detail `ActionSheet` option icons render `success`-colored Feather glyphs, a native `color` prop. **The rule stays literal: mirror an EXISTING token's RGB verbatim, never invent a new one here** — `success` was not a new semantic color, just newly needed as a native prop | `mobile/src/theme/nativeChromeColors.ts:NATIVE_CHROME_RGB,rgbFromTriplet` + `mobile/src/app/navigation/navigationTheme.ts:buildNavigationTheme` + `mobile/src/components/ui/{EmptyState.tsx,LoadingIndicator.tsx}` + `mobile/src/features/tasks/screens/TaskDetailScreen.tsx` (TSK-004 `success` consumer) | FND-003 (extended FND-005; `success` entry added TSK-004) | — |
| Shared UI primitives (reuse-or-block) | `EmptyState` / `LoadingIndicator` / `Screen` in `src/components/ui` (barrel `@/components/ui`) are the single implementation of each presentational concern — features **reuse**, never re-implement; a fresh re-implementation of one of these is a `[blocking]` code-review finding. Confirmed consumers: F-031 (ORG, no-search-results) reuses `EmptyState`; F-042 (TSK, pull-to-refresh) reuses `LoadingIndicator` | `mobile/src/components/ui/{EmptyState.tsx,LoadingIndicator.tsx,Screen.tsx,index.ts}` | FND-005 | — |
| App icon family = Feather (`FeatherIconName`) | `react-native-vector-icons/Feather` is the app-wide icon family (first use in FND-005, adopt app-wide — don't add a second icon font). Icon `name` props are typed via the `FeatherIconName` type-only alias (`keyof typeof import('react-native-vector-icons/glyphmaps/Feather.json')`), recovering a real union from the installed glyph map — reuse this alias for any future `icon` prop rather than re-deriving or falling back to a bare `string` | `mobile/src/components/ui/EmptyState.tsx:FeatherIconName` | FND-005 | — |
| Themed StatusBar (OS chrome, not nav chrome) | The OS status bar (glyph color + Android background) is rendered from `ThemeProvider`, not from the navigator — `NavigationContainer`'s `theme.dark` only styles React Navigation's own chrome (headers/tab-bar/background), it does not drive the OS status bar. Flips `barStyle`/`backgroundColor` with `resolvedScheme`, reusing the same `NATIVE_CHROME_RGB` source above, so it holds even before/without a navigator mounted | `mobile/src/theme/ThemeProvider.tsx:ThemeProvider` | FND-003 | — |
| Placeholder screen + RN-Navigation jest setup | Every unbuilt route renders a themed `<PlaceholderScreen name detail?>` (semantic tokens only) until its feature module replaces it — navigator wiring never changes when that swap happens, it only imports the screen component. Jest env fixes for anything that mounts through React Navigation/SafeAreaProvider are wired **once, globally** in `mobile/jest/setup.js` (registered in `jest.config.js` `setupFiles`) — currently the official `react-native-safe-area-context/jest/mock` (real `SafeAreaProvider` never fires initial-insets under Jest, so nested trees render `null` without it). Any future screen test that mounts through `AppProviders`/navigation reuses this setup file rather than re-mocking safe-area per test | `mobile/src/app/navigation/PlaceholderScreen.tsx` + `mobile/jest/setup.js` (wired via `mobile/jest.config.js:setupFiles`) | FND-003 | — |
| App bootstrap / first-launch seam (MMKV flag, single writer) — **write side now IMPLEMENTED (PRO-001)** | `hasLaunched` is one raw MMKV boolean key (`app.hasLaunched`), read via a guarded `readHasLaunched()` (try/catch, defaults `false` — survives a *throwing* read, not just a bad type, per FR6) and seeded once as `useLaunchStore`'s initial state. **`BootstrapScreen` (the `Splash` route) only READS the flag** to `navigation.reset` into `ProfileSetup` (false) or `Tabs` (true) — it never calls `setHasLaunched()` itself. `ProfileSetupScreen.onValid` is now the single seam that calls `setHasLaunched()`, and ONLY on genuine setup completion (never on mount) — a force-quit mid-setup correctly re-shows `ProfileSetup` next launch. **Load-bearing invariant: never add a second source of truth for "has this device launched before" — PRO writes through this store, it does not track its own flag.** | `mobile/src/core/store/launchStore.ts:useLaunchStore,readHasLaunched,setHasLaunched` + `mobile/src/app/navigation/BootstrapScreen.tsx:BootstrapScreen` + `mobile/src/features/profile/screens/ProfileSetupScreen.tsx:onValid` | FND-004 (write side closed by PRO-001) | — |
| RHF + Zod form pattern (ANCHOR) | `useForm({resolver: zodResolver(schema), mode: 'onBlur', defaultValues})` + one `Controller` per RN `TextInput` (never RHF's `register`, which assumes a DOM input) + `const onSubmit = handleSubmit(onValid)` so `onValid` only ever receives schema-valid data — the repository's throw-on-invalid write path is structurally unreachable from the form (STG coherence gap f). Inline errors read from `formState.errors.<field>?.message` and pass straight to `FormField`'s `error` prop. A local `hasSubmittedRef` (not just RHF's `isSubmitting`) guards against a same-JS-tick double-tap racing ahead of the re-render that disables the button. Every later form (TSK) copies this shape verbatim rather than re-deciding validation-boundary/error-display per screen. **Confirmed reused for an edit flow (PRO-002 `ProfileScreen`):** same `zodResolver`/`Controller`/boundary-validation shape, with `reset(profile)` seeding `defaultValues` from the current record on entering edit mode instead of an empty object — the one deliberate variance for "edit existing" vs. "create new" (PRO-002 omits the `hasSubmittedRef` double-tap guard — see stack.md spec-gap note, non-blocking) | `mobile/src/features/profile/screens/ProfileSetupScreen.tsx:ProfileSetupScreen` + `mobile/src/features/profile/screens/ProfileScreen.tsx:ProfileScreen` (edit mode) | PRO-001 (edit-flow variant confirmed PRO-002) | — |
| `Button` + `FormField` form primitives | `Button` (`{label,onPress,disabled?,loading?,fullWidth?}`) is the one primary-action recipe (formalizes the `bg-primary` CTA `EmptyState`'s action button already shipped, FND-005); `loading` swaps the label for an `ActivityIndicator` in-place (no height jump) and sets `accessibilityState={{disabled:true,busy:true}}`. `FormField` (`{label,error?,required?} & Pick<TextInputProps,…>`) is the one labeled-input-plus-inline-error shape for every RHF+Zod field app-wide — border flips to `border-danger` on `error`, error row uses the `alert-circle` Feather icon + `text-danger` text (never color-alone), with `accessibilityLiveRegion="polite"` (Android) / `AccessibilityInfo.announceForAccessibility` (iOS) so the error is actually announced. Both live in `components/ui` (barrel-exported) — TSK reuses, never re-implements; a fresh hand-rolled button/field shape is a `[blocking]` code-review finding, same reuse-or-block contract as `EmptyState`/`LoadingIndicator`/`Screen`. **Extended (TSK-002)** with a `multiline`/`numberOfLines` mode (adds `min-h-24` + `textAlignVertical: 'top'` when set) for the task-description field — backward-compatible, the single-line path is unchanged; reuse this mode for any future multi-line field rather than a second field component | `mobile/src/components/ui/{Button.tsx,FormField.tsx}` (barrel: `mobile/src/components/ui/index.ts`) + `docs/context/design-system.md` Component inventory | PRO-001 (multiline mode TSK-002) | — |
| Feature store hydrates from a repository (not raw MMKV) | A feature-owned Zustand store (`src/features/<x>/store`) holds only in-memory state (`profile: Profile \| null` + actions) and hydrates **once at store-init** by calling the STG repository directly (`hydrateProfile()` → `profileRepository.getProfile()`), not `core/services/storage.ts`/`createPersistedValue` and never raw `react-native-mmkv` — the repository already IS the STG hydration/persistence seam for that domain (STG-002). Every mutator (`setProfile`/`clearProfile`) writes through the repository FIRST, then updates in-memory state — the store is never the source of truth, the persisted domain is. TSK's task store follows this same shape (`taskRepository`, not a raw storage call). **Confirmed for a full action-set store (TSK-001):** `taskStore` extends this shape with 6 mutators (`addTask`/`updateTask`/`removeTask`/`toggleStatus`/`duplicateTask`/`refresh`), each a thin delegation that writes through `taskRepository` first (pure-transform, returns the new full collection) then `set`s it — no mutator recomputes `updatedAt` itself (that stays inside `taskRepository.upsertTask`, coherence check 2). TSK-002–005 extend this store, not fork it | `mobile/src/features/profile/store/profileStore.ts:useProfileStore,hydrateProfile` + `mobile/src/features/tasks/store/taskStore.ts:useTaskStore` | PRO-001 (confirmed TSK-001) | — |
| `TaskListItem` (task-row card, reuse-or-block) | The one task-row recipe app-wide — a `Pressable` card (`rounded-lg border border-border bg-card p-4`) with a leading 24dp toggle-checkbox (≥48dp hit target via `hitSlop`), title (strikethrough+muted+check-fill together when completed — never color-alone), optional due-date caption line, trailing overflow control. `onToggleComplete`/`onOpenActions` are both optional so TSK-001 could ship the row before either mutation existed to wire (TSK-004 supplies `onOpenActions`, same public-shape contract). Elevation resolves through `NATIVE_CHROME_RGB` (light: subtle shadow; dark: none, per anti-pattern #5). Home (TSK-001) and ORG's later filtered list both render this unchanged — a fresh hand-rolled row is `[blocking]`. **Extended (TSK-004):** the decorative trailing `chevron-right` is **replaced** by an `onOpenActions?: (task: Task) => void` "⋯" (`more-horizontal`) button that opens the row's `ActionSheet` menu — the row itself still owns navigate-to-Detail on tap; `TaskListItem` only calls the callback, the caller screen owns the sheet's visible/option state (same "dumb presentational component, screen owns interactive state" split as `Avatar`/`ActionSheet`) | `mobile/src/components/ui/TaskListItem.tsx:TaskListItem` + `docs/context/design-system.md` Component inventory | TSK-001 (`onOpenActions` extension TSK-004) | — |
| Cross-feature data read via core repository (never another feature's store) | A feature needing another feature's persisted data reads the shared **STG repository from `core`** directly (e.g. `HomeScreen` calls `profileRepository.getProfile()?.name` for the greeting) — never the other feature's Zustand store (`features/tasks` importing `features/profile`'s `useProfileStore` would be a `feature→feature` boundary violation; `eslint-plugin-boundaries` only allows same-feature-sibling + `core`/`components`/`theme`/`platform` imports). The repository read is already STG's total-read seam (absent→safe default, F-038), so no new seam is introduced and no profile state is duplicated. **Refinement:** re-read on `useFocusEffect`, not just on mount — a screen that stays mounted inside bottom-tabs (React Navigation doesn't unmount inactive tabs) would otherwise show a stale name after the user edits it on another tab. **Scope note — cross-feature only:** a screen reading/mutating its OWN feature's store (e.g. TSK screens reading `useTaskStore`, or ORG later reading the same TSK store) is unaffected and keeps using the store directly; this pattern applies only when the data belongs to a *different* feature | `mobile/src/features/tasks/screens/HomeScreen.tsx:HomeScreen` (`getProfile()` + `useFocusEffect`) | TSK-001 | — |
| `TaskForm` (shared create+edit feature form) | One shared form component per persisted-entity-with-multiple-entry-forms (create screen + edit screen): `useForm({resolver: zodResolver(schema.pick({...}))})` inside the form component itself (not the screen), props `{defaultValues?, onSubmit, submitLabel}` so a create screen passes no `defaultValues` (empty form) and an edit screen passes the existing record — same component, same validation, zero duplicated field/error JSX. Reuses the RHF+Zod anchor's `Controller`→`FormField` + `hasSubmittedRef` dup-guard shape verbatim (PRO-001), just hoisted one level so two screens share it instead of each re-declaring the form. **Reuse contract for TSK-003/TSK-005:** TSK-003 (edit) passes the loaded `Task` as `defaultValues` and a `submitLabel="Save"`; TSK-005 (due-date) adds a field by widening the `.pick({...})` set passed to `taskSchema`, not by forking a second form. A fresh hand-rolled create/edit form pair is a `[blocking]` code-review finding once this exists. **Caller-side edit-merge contract (confirmed TSK-003):** `TaskForm`'s field set is deliberately narrower than `Task` (today `.pick({title,description})`) — an edit screen merges the validated form values over the EXISTING record's fields the form doesn't own (`status`, `dueDate` today) before calling `updateTask`, because `upsertTask` wholesale-replaces on update (STG gap a); submitting only the form's fields would silently blank the rest. `EditTaskScreen` is the reference for this merge shape. TSK-005 shrinks this merge list by one (`dueDate` moves into the form's own `.pick`) — any field never added to the form stays the caller's responsibility to carry forward, never drop. **Scope note:** lives in `features/tasks/components`, not `components/ui` — it's a feature-scoped shared form, not an app-wide primitive; promote to `components/ui` only if a second feature needs the identical create+edit-share shape | `mobile/src/features/tasks/components/TaskForm.tsx:TaskForm` + `mobile/src/features/tasks/screens/EditTaskScreen.tsx:handleSubmit` (merge reference) | TSK-002 (edit-merge confirmed TSK-003) | — |
| Typed navigation inside a feature screen (no app-layer import) | `useNavigation()` (plain, from `@react-navigation/native`) resolves fully typed for free via the global `ReactNavigation.RootParamList` augmentation (`app/navigation/types.ts`) — no per-call generic, no `useAppNavigation` import (that helper is `app`-layer; `feature→app` is a disallowed `eslint-plugin-boundaries` direction). A route WITH params (`useRoute()`) has no equivalent default-generic mechanism, so the screen declares a **local, narrow** param-list type — `RouteProp<{RouteName: {param}}, 'RouteName'>` — instead of importing `RootStackParamList`/`useAppRoute` from `app/navigation`. `ProfileSetupScreen`/`ProfileScreen`/`HomeScreen`/`AddTaskScreen` already used the plain-`useNavigation()` half informally (each carries its own boundary-note comment, no route params needed); **TSK-003 is the first screen with route params and formalizes both halves as one registry row.** Every later feature screen with route params (ORG) copies this shape rather than re-deriving the boundary workaround per screen — a fresh screen that imports `useAppNavigation`/`useAppRoute` from `app/navigation` is a boundary violation the code-reviewer should flag even before the lint gate catches it. **Note:** with every feature screen deliberately avoiding it, `app/navigation/hooks.ts` (`useAppNavigation`/`useAppRoute`) is now confirmed single-consumer (`BootstrapScreen`, itself app-layer) — see `harness-debt.md` | `mobile/src/features/tasks/screens/TaskDetailScreen.tsx:TaskDetailParamList` + `mobile/src/features/tasks/screens/EditTaskScreen.tsx:EditTaskParamList` | TSK-003 (informal `useNavigation()`-only precedent since PRO-001) | — |
| Native splash (react-native-bootsplash) config | Both platforms wired end-to-end: Android `res/values/{colors.xml,styles.xml}` (`BootTheme`) + `AndroidManifest.xml` activity theme + `MainActivity.kt` (`RNBootSplash.init()` before `super.onCreate(null)` — the documented "screens < 4.16.0" integration, since this app pins `react-native-screens@4.8.0`); iOS `BootSplash.storyboard` + `AppDelegate.mm` (`customizeRootView:` → `RNBootSplash initWithStoryboard:rootView:`, the pre-RN-0.80 Obj-C `RCTAppDelegate` subclass form) + `Info.plist` (`UILaunchStoryboardName` = `BootSplash`). JS side hides it with `BootSplash.hide({fade: true})` once the boot decision (theme + launch flag) resolves. **This is the reference config for both platforms** — the current asset is still the library's placeholder mark; swapping in a real logo + running `bootsplash generate` is a deferred design follow-up, not yet done. | `mobile/android/app/src/main/res/values/{colors.xml,styles.xml}` + `mobile/android/app/src/main/java/com/todoapp/MainActivity.kt` + `mobile/ios/TodoApp/{BootSplash.storyboard,AppDelegate.mm,Info.plist}` | FND-004 | — |
| Persisted domain model (`core/types` + repository) | A persisted domain shape is declared exactly once — a Zod schema + its `z.infer` type in `core/types/<domain>.ts` — and the matching `core/services/<domain>Repository.ts` is the ONLY read/write path for it. Feature code (PRO/TSK) imports the type + calls the repository; it never calls `getItem`/`setItem` directly and never re-declares the shape (code-reviewer blocks both). This is how STG-001's typed storage service gets a concrete domain on top of it | `mobile/src/core/types/{profile.ts,task.ts}` + `mobile/src/core/services/{profileRepository.ts,taskRepository.ts}` | STG-002 | — |
| Repository pattern (total reads, validating writes, pure-transform mutations) | A repository's read is **total** (absent/corrupt → the caller's safe default via `getItem` — `null` for a singleton, `[]` for a collection — never throws); its write Zod-validates before persisting and throws on mismatch (a caller bug, e.g. an unvalidated form value, not corrupt storage — deliberately not swallowed). A collection repository adds `upsertTask(input)`/`removeTask(id)` as **pure transforms**: read the full collection, compute the next array, persist it, return it — callers never touch MMKV or hand-diff the array. `upsertTask` on an existing id is a **wholesale field replace**, not a partial patch — any edit-form caller must submit the full set of business fields, not just the changed ones | `mobile/src/core/services/taskRepository.ts:upsertTask,removeTask` + `mobile/src/core/services/profileRepository.ts:getProfile,saveProfile,clearProfile` | STG-002 | — |
| ID generation (`newId()`) | One id-generation util for the whole app, `core/lib/id.ts:newId()` (`react-native-uuid` v4 — pure JS, no native linking, no `react-native-get-random-values` polyfill). Any repository/store minting a fresh persisted-entity id calls this rather than hand-rolling a `Math.random()`/timestamp id. **Caveat — carry this whenever `newId()` is reused:** `react-native-uuid` is `Math.random()`-backed, **NOT a CSPRNG**. Use it only for internal/non-security entity ids (task ids, list-item keys). A security-sensitive token (auth, session, password-reset) needs a real CSPRNG (`uuid` + `react-native-get-random-values`, or `react-native-quick-crypto`) — never reuse `newId()` for that | `mobile/src/core/lib/id.ts:newId` | STG-002 | — |
| `SegmentedControl<T>` (single-select fixed-option control) | Generic `{options: {value: T; label: string}[]; value: T; onChange; accessibilityLabel}` (`T extends string`) — one themed track (`bg-surface`/`border`), 2–4 segments, selected = `bg-primary` fill + `text-primary-fg font-semibold` label, unselected = plain `text-text` regular (fill+weight change together, never color-alone). Each segment `accessibilityRole="tab"` + `accessibilityState={{selected}}`, track `accessibilityRole="tablist"`, ≥48dp via `min-h-12` per segment. First use: PRO-002's System/Light/Dark theme toggle. **Forward purpose — ORG reuses this verbatim for filter-chip / sort-option controls (F-023–029)**; a fresh hand-rolled fixed-option selector there is a `[blocking]` code-review finding | `mobile/src/components/ui/SegmentedControl.tsx:SegmentedControl` (barrel `mobile/src/components/ui/index.ts`) | PRO-002 | — |
| `Avatar` (identity chip) | Circular `bg-primary/10` chip (same soft-chip treatment as `EmptyState`'s icon chip) showing initials derived from `name` (first + last-word initial, uppercased; empty-name edge case falls back to a Feather `user` glyph) — two fixed size presets, `'md'` (64dp, reuses `EmptyState`'s exact icon-chip size) / `'lg'` (80dp, default). Decorative — hidden from the accessibility tree when adjacent name/email text already carries identity. **Extended (PRO-003) with a `photoUri?: string` prop** — renders an `Image` filling the chip in place of initials/icon when present, falling back to the initials/icon treatment on a missing/broken file uri (defensive, spec FR5). Extend this component further if a third avatar mode is ever needed — never fork a second avatar | `mobile/src/components/ui/Avatar.tsx:Avatar` (barrel `mobile/src/components/ui/index.ts`) | PRO-002 (photo variant PRO-003) | — |
| `ActionSheet` (themed cross-platform bottom sheet AND confirm dialog AND single-select menu) | One `Modal`-based options menu — dimmed backdrop (`bg-text/40`) + `rounded-t-lg bg-card` sheet, one row per `{label, onPress, destructive?, accessibilityLabel?, icon?, active?}` option plus a trailing Cancel; renders `null` (not just hidden) while `visible` is `false`. Built as ONE shared component rather than branching on iOS-only `ActionSheetIOS`, so the interaction + a11y/Maestro anchors (`menu`/`menuitem` roles) hold on both platforms. First use: `AvatarPhotoField`'s Take Photo / Choose from Library / Remove menu — any future options-menu reuses this rather than a second hand-rolled `Modal` (a fresh one is a `[blocking]` code-review finding, same reuse-or-block contract as the other `components/ui` primitives). **Extended (TSK-004) with two optional, backward-compatible props: `title?: string`** (an `accessibilityRole="header"` row above the options — makes `ActionSheet` double as a themed confirm/yes-no dialog, used for TSK-004's delete-confirm) **and `options[].icon?: FeatherIconName`** (a leading 20dp glyph per row, color-paired to the row's `destructive` state via the `nativeChromeColors` map). **Extended again (ORG-003, PR #20, review) with `options[].active?: boolean`** — a single-select indicator (used for ORG-003's Sort-by menu, marking the currently-active sort key): the active row renders its icon + label in `text-primary`/`font-semibold` (icon+weight+color together, never color-alone); `destructive` takes precedence over `active` if both were ever set on the same option (defensive, not expected in practice); reuses the pre-existing `NATIVE_CHROME_RGB.primary` — no new token. PRO-003/TSK-004 call sites (no `active`) are unaffected. **`ActionSheet` is now the app's ONE confirm-dialog + single-select-menu primitive — a bespoke `Alert.alert`/inline confirm `Modal`/single-select list is a `[blocking]` code-review finding**, same reuse-or-block contract as the rest of `components/ui`. **Non-blocking follow-up (ORG-003 review nit):** the active option could additionally set `accessibilityState={{selected:true}}` — currently mitigated by the trigger's own announcement + the semibold visual cue, not yet applied | `mobile/src/components/ui/ActionSheet.tsx:ActionSheet` (barrel `mobile/src/components/ui/index.ts`) | PRO-003 (`title`/`icon` extension TSK-004; `active` extension ORG-003) | — |
| Confirm-gated destructive action (shared feature hook + `ActionSheet`-as-confirm) | A destructive store mutation (delete) is **never** wired directly to a tap — it always routes through a small feature-owned hook that gates the mutation behind an explicit confirm step. Shape: `openMenu`/`closeMenu`/`menuTask` for the options sheet, `requestDelete`/`cancelDelete`/`confirmDelete`/`deleteTarget` for the confirm — `confirmDelete` only ever calls the store mutation when a `deleteTarget` was captured via `requestDelete` first, so the destructive call is structurally unreachable from a raw tap. The confirm UI is the `ActionSheet` `title` extension above (never a second confirm mechanism). An optional `onDeleteConfirmed(task)` callback lets a call site add a post-delete side-effect (`TaskDetailScreen` passes `goBack`) without the hook knowing about navigation — this is what let ONE hook instance-shape serve both a list row (no navigation after delete) and a detail screen (navigate back after delete). **Reuse contract:** any future confirm-gated destructive action (ORG bulk-actions, a future clear-profile/reset flow — PRO coherence gap e) copies this hook shape + the `ActionSheet`-with-`title` confirm, rather than a bespoke `Alert.alert` or inline confirm state | `mobile/src/features/tasks/hooks/useTaskActions.ts:useTaskActions` | TSK-004 | — |
| Native image-picker pattern (single normalized call site) | `features/profile/lib/photoPicker.ts` is the ONLY module that imports `react-native-image-picker` — `pickPhotoFromCamera()`/`pickPhotoFromLibrary()` both return a closed `PhotoPickResult` (`{status:'success',uri}` \| `{status:'cancelled'}` \| `{status:'error',reason}`), normalizing the library's `didCancel`/`errorCode` response shape (never a rejection — permission-denied/cancel/unavailable are all *resolved* outcomes, not exceptions) so callers never touch the library directly or see bytes/base64. `AvatarPhotoField` (the point-of-use component) owns permission-denied/unavailable/error **messaging**, and hands back only a plain uri string (or `undefined` on Remove) via `onPhotoChange` — persistence stays through `profileRepository` (STG coherence gap f), never inside the picker/field. Any future native-picker/permission surface (camera, files, contacts) copies this shape: one `lib/` call-site module returning a closed result type, one field/screen component owning the UX + messaging, zero direct library imports elsewhere | `mobile/src/features/profile/lib/photoPicker.ts:pickPhotoFromCamera,pickPhotoFromLibrary,PhotoPickResult` + `mobile/src/features/profile/components/AvatarPhotoField.tsx:AvatarPhotoField` | PRO-003 | — |
| Due-date formatting util (`core/lib/formatDueDate.ts`) | ONE due-date formatting source app-wide — `formatDueDateCompact` (`'MMM d, h:mm a'`, list-row caption) + `formatDueDateFull` (`'MMM d, yyyy · h:mm a'`, detail-row/form-trigger single-record display); no ad-hoc `date-fns` format string anywhere else. **Placement rule (reusable beyond this util):** a formatter/util consumed by BOTH `components/ui` and `features/*` MUST live in `core/lib`, never a feature `lib/` — `components → features` is a disallowed `eslint-plugin-boundaries` direction, so `core/lib` (the one layer both sides may import) is the only legal home, same reasoning as `core/lib/id.ts`. Consolidates 3 previously-disagreeing per-file constants (`TaskListItem`'s old `'MMM d'`, `TaskDetailScreen`'s old `'MMM d, yyyy'`, plus this task's new trigger, which would otherwise have invented a 4th) | `mobile/src/core/lib/formatDueDate.ts:formatDueDateCompact,formatDueDateFull` | TSK-005 | — |
| `IconButton` (48dp icon-only button, reuse-or-block) | One themed 48dp outlined-square icon-only button (`min-h-12 min-w-12 items-center justify-center rounded-md border border-border`) for any icon-only tap target — promoted off `TaskDetailScreen`'s single-use `ICON_BUTTON_BASE_CLASSNAME` local const (TSK-004 watch item) the moment a 2nd consumer needed the identical shape (`DueDateField`'s clear affordance, TSK-005); `TaskDetailScreen`'s Toggle/More buttons were refactored onto it too. A fresh hand-rolled icon-only `Pressable` is now a `[blocking]` code-review finding, same reuse-or-block contract as the rest of `components/ui`. 10th `components/ui` primitive | `mobile/src/components/ui/IconButton.tsx:IconButton` (barrel `mobile/src/components/ui/index.ts`) | TSK-004 (promoted to a shared primitive TSK-005) | — |
| Native date+time picker (per-platform, one field component) | One field component (`DueDateField`) owns the platform split the native module forces: **iOS** renders the picker inline via a single combined `mode="datetime"` (one native control, one step); **Android** has no combined mode, so it opens `DateTimePickerAndroid.open()` sequentially — the date picker first, whose `onChange` opens the time picker, only committing the merged value once both resolve. Both platforms emit the field's value as `.toISOString()` (never a bare date string — Zod requires full ISO-8601, STG gap b) or `undefined` on Clear (never `null`); dismiss/cancel on either native dialog leaves the existing value untouched. Any future date/time surface (e.g. a reminder time) copies this component's platform-split shape rather than re-deriving it | `mobile/src/features/tasks/components/DueDateField.tsx:DueDateField` | TSK-005 | — |
| Derived selector over a store (search → filter → sort pipeline) — **PIPELINE COMPLETE** | A filtered/sorted VIEW over a Zustand store's array is a **pure** `(items, query) => items` function in the feature's `lib/` — never a store mutation, never a second copy of the array. Structure: a predicate/comparator map keyed by the query field (`Record`/`Partial<Record>`), with a safe fallback for a not-yet-wired key (`resolveComparator` falls back to the default comparator rather than throwing on an unrecognized persisted value); `.slice()` before `.sort()` (guards `Array.sort`'s in-place mutation); each pipeline stage isolated in its own function so a LATER task can swap one stage's implementation without touching the pipeline shape. The store's array stays the persistence-order source of truth — the selector's output order is a view concern only. **All 3 seams now filled (ORG-003, PR #20, review):** `SORT_COMPARATORS` — `due` (ascending, **no-`dueDate` sorts LAST**), `alpha` (`localeCompare`, `sensitivity:'base'` — case-insensitive A→Z), `updated` (`updatedAt` descending), `created-desc` (unchanged default). **Any future sort surface reuses these exact comparator conventions** (no-dueDate-last, `localeCompare` base-sensitivity, desc-by-timestamp) rather than re-deriving them | `mobile/src/features/tasks/lib/selectVisibleTasks.ts:selectVisibleTasks,FILTER_PREDICATES,SORT_COMPARATORS,resolveComparator,matchesSearch` | ORG-001 (`matchesSearch` filled ORG-002, PR #19; `SORT_COMPARATORS` filled ORG-003, PR #20, review — pipeline now complete, no seams left) | — |
| UI-query-state persisted via the typed storage service (NOT raw-key) | A **new** persisted UI preference (not domain data) — even one that superficially resembles `themeStore`/`launchStore`'s single-flag shape — routes through `createPersistedValue(StorageKeys.<key>, schema, fallback)` as one Zod-validated envelope, exactly like a domain repository would, rather than a fresh raw MMKV key. A **multi-field** preference (e.g. `{filter,sort}`) persists as ONE envelope via a single `setItem`/`getItem` call, not one key per field — so a partial write can never leave one field updated and the other stale (`setFilter`/`setSort` both read-then-rewrite the full envelope). This is the concrete example that closes the "row above is superseded, use the typed service" guidance for a real new preference — the first one since STG-002 | `mobile/src/features/tasks/store/taskQueryStore.ts:useTaskQueryStore,taskQueryPrefsSchema,hydrateTaskQueryPrefs` + `mobile/src/core/services/storage.ts:StorageKeys.taskQuery` | ORG-001 | — |
| Untrusted search-input handling (ReDoS-safe) | User-typed search/filter text is **never** compiled into a `RegExp`: `search.trim().toLowerCase().slice(0, 128)` (length-capped) then a plain, case-insensitive `String.includes` against the searched fields — the closed resolution of the ORG-001 forward security advisory. A `RegExp`/`.match()` built from raw user input is a `[blocking]` code-review finding (ReDoS risk holds even in a fully local/offline app — a pathological pattern can still hang the JS thread). `SearchBar` (`FormField` chrome + a leading decorative Feather `search` icon + `IconButton` clear, props `value`/`onChangeText`/`onClear`) is the reference search-input UI to reuse-or-adapt for any future search surface — deliberately **feature-local** (`features/tasks/components`), not yet promoted to `components/ui` (no 2nd consumer yet) | `mobile/src/features/tasks/lib/selectVisibleTasks.ts:matchesSearch` + `mobile/src/features/tasks/components/SearchBar.tsx:SearchBar` | ORG-002 | — |
