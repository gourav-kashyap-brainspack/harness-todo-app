# Design System — {{APP_DISPLAY_NAME}}

> Owned by the **design** agent; implemented by **frontend**. Code is the source of truth
> (NativeWind tokens: `mobile/tailwind.config.js` + `global.css` + `src/theme` + owned `src/components/ui`). Stub until the first UI module (UIF).

## Design intent
_(the product's aesthetic point of view — set at the first UI work; see Design Profile in `.claude/harness.config.md`)_

## Tokens
_(color / spacing / type scale — populated by the design agent)_

## Component inventory
_(owned UI primitives — grown per UI task)_

## Anti-patterns / banned defaults
_(things this product deliberately does NOT do — guards against AI-slop)_
