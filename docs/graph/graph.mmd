%% Task/build dependency graph — generated by the task-graph skill from docs/graph/modules.json.
%% Empty until /scope + /module populate the modules and tasks.
graph TD
  empty["No modules yet — run /scope"]
