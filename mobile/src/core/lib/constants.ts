/**
 * Cross-cutting constants (core/lib).
 * Kept trivial in FND-001 — proves the `@/*` path alias resolves at
 * type-check, bundle, and test time. Later tasks may extend this file.
 */
export const APP_NAME = 'Todo App';
